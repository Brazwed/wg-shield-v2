=== D5: get_container_status mock ===

Test 1 - Container running:
  docker inspect returns State.Running=true → "running" ✓

Test 2 - Container stopped:
  docker inspect returns State.Running=false → "stopped" ✓

Test 3 - Container missing:
  docker inspect returns error → "missing" ✓

Test 4 - Docker not available:
  has_docker fails → "unknown" ✓

Test 5 - Backward compat (existing callers):
  grep -q "running" → matches "running" only ✓
  [ "$st" = "running" ] → matches "running" only ✓
  [ "$st" = "stopped" ] → matches "stopped" only (not "missing") ✓
  else branch → catches both "stopped" and "missing" ✓

=== D8: _check_firewall_status mock ===

Test 1 - UFW active:
  FW_TYPE=ufw, ufw status active → returns 0 ✓

Test 2 - UFW inactive:
  FW_TYPE=ufw, ufw status inactive → returns 1 ✓

Test 3 - iptables with SSH+conntrack:
  iptables DROP present, conntrack ACCEPT present, SSH ACCEPT present → returns 0 ✓

Test 4 - iptables DROP but no WG-Shield rules:
  iptables DROP present, but no conntrack or SSH rule → returns 1 ✓

Test 5 - iptables ACCEPT-only (no DROP):
  No DROP/REJECT in INPUT → returns 1 ✓

=== D2/D3: run_with_spinner / spinner_wait ===

Test 1 - Command succeeds:
  run_with_spinner "msg" sleep 0.3 → shows spinner, exits 0 ✓

Test 2 - Command fails:
  run_with_spinner "msg" false → shows spinner, exits non-zero ✓

Test 3 - PID tracking:
  spinner_wait checks $! not $$ → correct PID ✓

Test 4 - No overlap with interactive commands:
  install_docker uses confirm() directly, not spinner ✓

=== D6/D7: mod_limits ===

Test 1 - First run (no existing config):
  Writes to limits.conf, runs ulimit -n, logs success ✓

Test 2 - Second run (already configured):
  Returns early with info message ✓

Test 3 - ulimit fails:
  ulimit -n 65535 fails → warns HARDEN_LIMITS_SESSION_WARN, continues ✓

Test 4 - ulimit succeeds:
  ulimit -n 65535 succeeds → no warning ✓

=== D10: running_names ===

Test 1 - No running_names variable left in code ✓

Test 2 - has_running boolean provides same early-warning function ✓
